/**
 * Title of Project
 * Author Name
 * 
 * learing what variables does
 */

"use strict";

/**
 * Create canvas
*/
function setup() {
    createCanvas(900, 900);
}


/**
 * OOPS I DIDN'T DESCRIBE WHAT MY DRAW DOES!
*/
function draw() {
    background(12);
    //drawing circle
    push;
    noStroke();
    fill(mouseX, mouseY, 0);
    ellipse(width / 2, height / 2, mouseX, );
    pop;

}